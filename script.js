// Sélectionne le bouton Shop
const shopButton = document.querySelector('.shop-button');

// Ajoute une action au clic
shopButton.addEventListener('click', () => {
    window.location.href = 'shop.html';  // Redirige vers la page Shop
});
